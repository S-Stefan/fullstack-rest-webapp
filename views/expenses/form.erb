<form action="/<%= @expense.id %>" method="POST">

  <div>
    <label>Activity</label>
    <input type="text" name="activity" placeholder="Please enter an activity" value=<%= @expense.activity%>>
  </div>

  <div>
    <label>Description</label>
    <textarea name="description" placeholder="Describe the activity"><%= @expense.description%></textarea>
  </div>

  <div>
    <label>Cost</label>
    <textarea name="cost" placeholder="Activity cost"><%= @expense.cost%></textarea>
  </div>

  <div>
    <label>Date</label>
    <textarea name="date" placeholder="Activity date"><%= @expense.date%></textarea>
  </div>

  <% if @expense.id %>
    <input type="hidden" name="_method" value="PUT">
  <% end %>
  <div>
    <input type="submit" value="Save Activity">
  </div>

</form>
